{% extends '.travis.yml.twig' %}

{% block env %}
env:
  - SYMFONY_VERSION=2.7.*
  - SYMFONY_VERSION=2.8.*
  - SYMFONY_VERSION=3.*

{% endblock env %}

{% block before_install %}
{{ parent() }}  - composer require "symfony/symfony:${SYMFONY_VERSION}" --update-with-dependencies
{% endblock before_install %}
