<?xml version="1.0"?>
<ruleset name="{{ name }}">
    <file>{{ autoload.folders.source }}</file>
    <file>{{ autoload.folders.test.phpunit }}</file>
    <file>{{ autoload.folders.test.functional.behat_context }}</file>

    <arg value="p"/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>

    <rule ref="PSR2"/>
</ruleset>
